apiVersion: v1
kind: Service
metadata:
  labels:
    app: chats
  name: chats
spec:
  ports:
    - name: 'http'
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: chats
  type: NodePort
